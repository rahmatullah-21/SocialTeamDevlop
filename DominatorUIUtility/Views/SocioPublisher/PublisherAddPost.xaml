<UserControl x:Class="DominatorUIUtility.Views.SocioPublisher.PublisherAddPost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DominatorUIUtility.Views.SocioPublisher"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"            
             mc:Ignorable="d" >
    <Grid>
        <StackPanel>
            <Label Content="{DynamicResource langAddSinglePost}" FontWeight="SemiBold" Margin="5"/>
            <TextBox x:Name="txtMessege" Text="{Binding Messeges,Mode=TwoWay}" AcceptsReturn="True" mah:TextBoxHelper.Watermark="{DynamicResource langTypeMessageHere}"
                     VerticalScrollBarVisibility="Auto" Margin="25,5" HorizontalAlignment="Left" TextWrapping="Wrap" Width="300" Height="80"/>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="{DynamicResource langAttachPhotos}"/>
                <Button  VerticalAlignment="Center" x:Name="btnPhotos"  Background="{DynamicResource AccentBaseColorBrush}" Foreground="White" >
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="15" Height="15" Fill="White">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Visual="{StaticResource appbar_camera}" Stretch="Fill"/>
                                </Rectangle.OpacityMask>
                            </Rectangle>
                            <TextBlock Text="{DynamicResource langPhoto}" Margin="6,0"/>
                            <TextBlock Text="{Binding ImageCount,StringFormat={} ( {0} )}">
                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ImageCount}" Value="0">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button  VerticalAlignment="Center" x:Name="btnVideo"  Background="{DynamicResource AccentBaseColorBrush}" Foreground="White" Margin="10,0" >
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="15" Height="15" Fill="White">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Visual="{StaticResource appbar_video}" Stretch="Fill"/>
                                </Rectangle.OpacityMask>
                            </Rectangle>
                            <TextBlock Text="{DynamicResource langVideos}" Margin="6,0"/>
                            <TextBlock Text="{Binding VideoCount,StringFormat={} ( {0} )}">
                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding VideoCount}" Value="0">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>

                            </TextBlock>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
            <ListView ItemsSource="{Binding LstMediaSources}" Name="LstviewImages" Margin="10,5">
                <ListView.Style>
                    <Style TargetType="{x:Type ListView}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Path=LstMediaSources.Count}" Value="0">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Style>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" VirtualizationMode="Recycling"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="8">
                            <MediaElement Source="{Binding}" Width="50" Height="50">
                                <MediaElement.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Name="MenuDeleteSingle" VerticalContentAlignment="Center" >
                                            <MenuItem.Header>
                                                <StackPanel Orientation="Horizontal">
                                                    <Rectangle Width="15" Height="20" Fill="Black" >
                                                        <Rectangle.OpacityMask>
                                                            <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_delete}" />
                                                        </Rectangle.OpacityMask>
                                                    </Rectangle>
                                                    <TextBlock Text="{DynamicResource langDelete}" Margin="5,0"/>
                                                </StackPanel>
                                            </MenuItem.Header>
                                        </MenuItem>
                                    </ContextMenu>
                                </MediaElement.ContextMenu>
                            </MediaElement>

                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="{DynamicResource langTitle}"/>
                <TextBox Text="{Binding Titles}" Width="400" HorizontalAlignment="Right" mah:TextBoxHelper.Watermark="{DynamicResource langTitleWaterMark}" x:Name="txtTitle"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsChecked="{Binding IsUseFacebookSellPostChecked}" Content="{DynamicResource langUseFacebookSellPost}" Margin="5"/>
                <Rectangle Width="15" Height="15" Fill="#003569" Margin="5">
                    <Rectangle.OpacityMask>
                        <VisualBrush Visual="{StaticResource appbar_social_facebook_variant}" Stretch="Fill"/>
                    </Rectangle.OpacityMask>
                </Rectangle>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsChecked="{Binding IsUseInstagramStoryPollChecked}" Content="{DynamicResource langUseInstagramStoryPoll}" Margin="5" />
                <Rectangle Width="15" Height="15" Fill="Red" Margin="5">
                    <Rectangle.OpacityMask>
                        <VisualBrush Visual="{StaticResource Instagram}" Stretch="Fill"/>
                    </Rectangle.OpacityMask>
                </Rectangle>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Button Content="{DynamicResource langAddMacros}" x:Name="btnAddTokens" FontWeight="Bold" Margin="10,5" Background="Transparent" Foreground="{DynamicResource AccentBaseColorBrush}"/>
                <Button Content="{DynamicResource langInstagramAdvancedOptions}" x:Name="btnInstagramAdvancedOptions"  FontWeight="Bold" Margin="10,5" Background="Transparent" Foreground="{DynamicResource AccentBaseColorBrush}" />
                <Button Content="{DynamicResource langAdvancedOptions}" x:Name="btnAdvancedOptions" FontWeight="Bold" Margin="10,5" Background="Transparent" Foreground="{DynamicResource AccentBaseColorBrush}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
