<UserControl x:Class="LegionUIUtility.CustomControl.EditProfile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:validation="clr-namespace:DominatorHouseCore.Utility;assembly=LegionCore"  
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls">
    <Grid VerticalAlignment="Top" Margin="5,10">
        <Grid.Resources>
            <ControlTemplate x:Key="ErrorTemplate">
                <StackPanel Orientation="Horizontal">
                    <AdornedElementPlaceholder x:Name="ErrorHolder" >
                        <Border BorderBrush="red" BorderThickness="1" />
                    </AdornedElementPlaceholder>
                    <TextBlock  VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0" Text="{Binding ElementName=ErrorHolder, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
                </StackPanel>
            </ControlTemplate>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="2.5*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="5*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2.5*" />
            </Grid.ColumnDefinitions>

            <StackPanel  Grid.Column="1" Orientation="Horizontal" Margin="30,0,0,0">
                <Ellipse Height="70" Width="70" >
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding EditProfileModel.ProfilePicPath}"/>
                    </Ellipse.Fill>
                </Ellipse>
                <StackPanel>
                    <TextBlock Text="{Binding EditProfileModel.Username}" Width="120" Margin="5" FontSize="13" FontWeight="SemiBold" Foreground="Black" VerticalAlignment="Center" />
                    <Button x:Name="btnUploadImage" Width="150" Background="Transparent" Content="Upload Image" x:FieldModifier="public" Margin="0,0,10,10"  Foreground="{DynamicResource AccentBaseColorBrush}" Command="{Binding UploadPhotoCommand}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2.5*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{DynamicResource LangKeyName}" Width="120" Margin="5" FontSize="13" FontWeight="SemiBold" Foreground="Black" VerticalAlignment="Center" />
            <TextBox Name="txtName"  Text="{Binding EditProfileModel.Fullname}"
                     controls:TextBoxHelper.Watermark="Type Name" Width="230" FontSize="13" FontWeight="SemiBold" 
                     Margin="5" Foreground="Black" VerticalAlignment="Center"  Height="30" Grid.Column="1" 
                     KeyDown="OnKeyDown"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2.5*" />
            </Grid.ColumnDefinitions>
            <TextBlock Name="txtblockUserName" Text="{DynamicResource LangKeyUsername}" Width="120" Margin="5" FontSize="13" FontWeight="SemiBold" Foreground="Black" VerticalAlignment="Center" />
            <TextBox Name="txtUserName"  Validation.ErrorTemplate="{StaticResource ErrorTemplate}" 
                     controls:TextBoxHelper.Watermark="Type User Name" Width="230" FontSize="13" FontWeight="SemiBold" 
                     Margin="5" Foreground="Black" VerticalAlignment="Center"  Height="30" Grid.Column="1" 
                     KeyDown="OnKeyDown">
                <TextBox.Text>
                    <Binding Path="EditProfileModel.Username" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validation:ValidationHelper Sender="txtUserName" ValidatesOnTargetUpdated="True"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2.5*" />
            </Grid.ColumnDefinitions>
            <TextBlock  Text="{DynamicResource LangKeyWebsite}" Width="120" Margin="5" FontSize="13" FontWeight="SemiBold" Foreground="Black" VerticalAlignment="Center" />
            <TextBox Name="txtWebSite" Text="{Binding EditProfileModel.ExternalUrl}"
                     controls:TextBoxHelper.Watermark="Type Website" Width="230" FontSize="13" FontWeight="SemiBold" 
                     Margin="5" Foreground="Black" VerticalAlignment="Center"  Height="30" Grid.Column="1" 
                     KeyDown="OnKeyDown"/>
        </Grid>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2.5*" />
            </Grid.ColumnDefinitions>
            <TextBlock  Text="{DynamicResource LangKeyBio}" Width="120" Margin="5" FontSize="13" FontWeight="SemiBold" Foreground="Black" VerticalAlignment="Center" />
            <TextBox Name="TxtBio" AcceptsReturn="True" TextWrapping="Wrap"  VerticalScrollBarVisibility="Auto"
                     controls:TextBoxHelper.Watermark="Type Bio" Width="230" FontSize="13" FontWeight="SemiBold" 
                     Margin="5" Foreground="Black" VerticalAlignment="Center"  Height="80" Grid.Column="1" 
                     KeyDown="OnKeyDown" Text="{Binding EditProfileModel.Bio}"/>
        </Grid>
        <CheckBox Name="ChkPrivateInfo" Margin="5" IsChecked="False" MinWidth="200" Grid.Row="5" Content="{DynamicResource LangKeyPrivateInformation}" HorizontalAlignment="Left"  Width="120" FontWeight="SemiBold" />

        <Grid Grid.Row="6"  Name="PrivateInfo" Margin="0,0,0,10" 
              Visibility="{Binding IsChecked,ElementName=ChkPrivateInfo,Converter={StaticResource BooleanToVisibilityConverter},Mode=TwoWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="80" />

            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2.5*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource LangKeyEmail}" Width="120" Margin="5" FontSize="13" FontWeight="SemiBold" Foreground="Black" VerticalAlignment="Center" />
                <TextBox Name="txtEmail" Text="{Binding EditProfileModel.Email}"  KeyDown="OnKeyDown"
                          Width="230" FontSize="13" Margin="5" FontWeight="SemiBold" Foreground="Black" VerticalAlignment="Center"  Height="30" Grid.Column="1">

                </TextBox>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2.5*" />
                </Grid.ColumnDefinitions>
                <TextBlock  Text="{DynamicResource LangKeyPhoneNumber}" Margin="5" Width="120" FontSize="13" FontWeight="SemiBold" Foreground="Black" VerticalAlignment="Center" />
                <TextBox Name="txtPhone" Text="{Binding EditProfileModel.PhoneNumber}" KeyDown="OnKeyDown"
                         controls:TextBoxHelper.Watermark="Type Phone number" Width="230" FontSize="13" FontWeight="SemiBold" Margin="5" Foreground="Black" VerticalAlignment="Center"  Height="30" Grid.Column="1"/>
            </Grid>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2.5*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource LangKeyGender}" Width="120" Margin="5" FontSize="13" FontWeight="SemiBold" Foreground="Black" VerticalAlignment="Center" />
                <StackPanel Grid.Column="1">
                    <RadioButton GroupName="gender" Margin="5" Content="{DynamicResource LangKeyMale}" IsChecked="{Binding EditProfileModel.IsMaleChecked}"/>
                    <RadioButton GroupName="gender"  Margin="5" Content="{DynamicResource LangKeyFemale}" IsChecked="{Binding EditProfileModel.IsFemaleChecked}"/>
                    <RadioButton GroupName="gender"  Margin="5" Content="{DynamicResource LangKeyNotSpecified}" IsChecked="{Binding EditProfileModel.IsNonSpecifiedChecked}"/>
                </StackPanel>
            </Grid>
        </Grid>
        <Grid Grid.Row="7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                <Button x:Name="btnSubmit" x:FieldModifier="public" Margin="0,0,10,10"  Foreground="White" Content="{DynamicResource LangKeySubmit}">
                    <Button.Style>
                        <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                            <Setter Property="IsEnabled" Value="false" />
                            <Setter Property="Background" Value="Gray"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding ElementName=txtUserName, Path=(Validation.HasError)}" Value="false" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="IsEnabled" Value="true" />
                                    <Setter Property="Control.Background" Value="Green"/>
                                </MultiDataTrigger>

                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>



            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
