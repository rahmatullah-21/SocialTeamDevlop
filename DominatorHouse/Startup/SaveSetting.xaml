<UserControl x:Class="DominatorHouse.Startup.SaveSetting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DominatorHouse.Startup"
             mc:Ignorable="d" x:Name="SaveSettingUi">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl Content="{Binding SourceUserControl}"/>
        <Button Height="40" x:Name="BtnNext" HorizontalAlignment="Right" Foreground="White"
                Grid.Row="1" Margin="8" 
                Command="{Binding NextCommand}" CommandParameter="{Binding ElementName=BtnNext}">
           <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                    <Setter Property="Content" Value="{DynamicResource LangKeyNext}"/>
                    <Setter Property="Background" Value="{DynamicResource AccentBaseColorBrush}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedIndex}" Value="2">
                            <Setter Property="Content" Value="{DynamicResource LangKeyFinish}"/>
                            <Setter Property="Control.Background" Value="Green"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
        <Button Height="40" HorizontalAlignment="Left"  Grid.Row="1" Margin="8" Foreground="White"
                Content="{DynamicResource LangKeyPrevious}" Command="{Binding PreviousCommand}">
           
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                    <Setter Property="IsEnabled" Value="True"/>
                    <Setter Property="Background" Value="{DynamicResource AccentBaseColorBrush}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedIndex}" Value="0">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Control.Background" Value="Gray"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

    </Grid>
</UserControl>
